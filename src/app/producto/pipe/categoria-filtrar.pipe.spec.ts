import { CategoriaFiltrarPipe } from './categoria-filtrar.pipe';

describe('CategoriaFiltrarPipe', () => {
  it('create an instance', () => {
    const pipe = new CategoriaFiltrarPipe();
    expect(pipe).toBeTruthy();
  });
});
