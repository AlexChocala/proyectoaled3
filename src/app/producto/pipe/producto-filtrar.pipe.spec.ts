import { ProductoFiltrarPipe } from './producto-filtrar.pipe';

describe('ProductoFiltrarPipe', () => {
  it('create an instance', () => {
    const pipe = new ProductoFiltrarPipe();
    expect(pipe).toBeTruthy();
  });
});
